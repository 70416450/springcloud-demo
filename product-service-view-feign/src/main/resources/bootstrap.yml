spring:
  cloud:
    config:
      label: master
      profile: test    #本次访问的配置项
      discovery:
        enabled: true
        serviceId: config-server
    bus:
      enabled: true
      trace:
        enabled: true
rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest